<!DOCTYPE html>
<html>
<head>
</head>

<body>
<div class="container-fluid padding" style="height:64px;"></div>

<!-- Test Session Heading -->
<div class="container-fluid padding">
<div class="row welcome text-center mb-2">
  <div class="col-12">
		<h1 class="display-4">Test Session</h1>
    <h1 class="display-4"><%= @notebook.name %></h1>
		<h1 class="display-4"><%= @test.name %></h1>
  </div>
</div>
</div>

<div class="d-flex flex-row flex-wrap justify-content-center mx-auto w-100 h-100">
	<div id="carouselExampleControls" class="carousel slide w-100" data-ride="carousel" data-interval="false">
		<div class="carousel-inner">
			<% first_found = false %>
			<% @questions.each do |question| %>
				<div class="carousel-item <% if !first_found %> active <% end %> <% first_found=true %>">
					<div class="d-block mx-4">
						<%= render :template => "layouts/_item_entry", :locals => { :question => question, :entry_type => "question" } %>
					</div>
				</div>
			<% end %>
		</div>
		<a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
			<span class="carousel-control-prev-icon bg-dark" aria-hidden="true"></span>
			<span class="sr-only">Previous</span>
		</a>
		<a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
			<span class="carousel-control-next-icon bg-dark" aria-hidden="true"></span>
			<span class="sr-only">Next</span>
		</a>
	</div>
</div>

<div class="container-fluid padding" style="height:64px;"></div>
</body>
</html>