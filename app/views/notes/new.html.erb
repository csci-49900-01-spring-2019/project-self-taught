<!DOCTYPE html>
<html>
  <head>
  </head>

  <body>
    New Note
    <%= form_for(@note, url: notebook_notes_path) do |f| %>
      <p><%= f.label :name %> <%= f.text_field :name %></p>
      <p><%= f.submit %></p>
    <% end %>
  </body>
</html>
